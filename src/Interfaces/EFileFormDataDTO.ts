interface EFileFormDataDTO {
    name: string;
    orgId: string;
    caseId: string;
    // recordKey: string;
    key: string;
    // loginId: string;
    formSequence: number;
    jwtToken: string;
}